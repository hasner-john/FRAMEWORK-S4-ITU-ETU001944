# FRAMEWORK-S4-ITU-ETU001944
Projet de création de framework de A à Z.

## TODO
[x] - Sprint0 : FrontControllerServlet (peut importe l'url en requette ca redirige dans )
        + proccessRequest(...)
        + output(url)